apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: mygcp-ingress
spec:
  rules:
  - http:
      paths:
      - path: /*
        backend:
          serviceName: employee
          servicePort: 8080
      - path: /dept/*
        backend:
          serviceName: department
          servicePort: 8080
      